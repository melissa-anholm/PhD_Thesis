% !TEX root = ../thesis_main.tex

% Colors that have names associated with them get defined in ColorNamesMacros.tex

% Colors!  
\usepackage{color}
\usepackage{xcolor}
% Colors by name.  For use with the 'color' package.  ...no, these actually go with the 'xcolor' package.
\definecolor{darkpurple}  {rgb}{0.50,0,0.80}      % Purple.  Too dark to contrast with black text.
\definecolor{purple}      {rgb}{0.65,0.35,1.0}    % Purple.

\definecolor{spring}    {rgb}{0.6,0.9,0.2}    % Spring.  (it's like lime, but greener).
\definecolor{lime}      {rgb}{0.7,0.95,0.10}  % Lime.
\definecolor{fluorescentgreen}   {rgb}{0,0.9,0}        % Flourescent green.  Like silly putty.
\definecolor{green}     {rgb}{0,0.68,0}       % Green.  Medium green.
%\definecolor{darkgreen}{rgb}{0,0.4,0}        % Dark Green
\definecolor{magenta}   {rgb}{0.8,0.0,0.6}    % Magenta.  
%\definecolor{orange}   {rgb}{1.0,0.5,0.0}    % Orange.  Pre-defined.
\definecolor{dgrey}     {rgb}{0.3,0.3,0.3}    % Dark Grey.
\definecolor{grey}      {rgb}{0.5,0.5,0.5}    % Grey.
\definecolor{lgrey}     {rgb}{0.7,0.7,0.7}    % Light Grey.
\definecolor{darkblue}  {rgb}{0.0,0.0,0.70}   % Dark Blue
\definecolor{darkred}   {rgb}{0.75,0.0,0.0}   % Dark Red
\definecolor{orangered} {rgb}{1.0,0.30,0.0}   % Orangered
\definecolor{white}     {rgb}{1.0,1.0,1.0}    % It's just white.


% "red" is already a color.
% "orange" is already a color.
% "yellow" is already a color (it's hard to read)
% "cyan" is already a color (it's light blue.)
% "blue" is already a color.
% "violet" is already a color (it's pretty dark)

% Colors with a purpose.
%\definecolor{commentcolor}{rgb}{0.58,0,0.82}   % Purple, currently.
%\definecolor{commentcolor}{rgb}{0.8,0.0,0.6}   % Magenta.
%\definecolor{commentcolor}{rgb}{0.1,0.1,0.80}  % Blue
%\definecolor{rownumcolor}{rgb}{0.5,0.5,0.5}    % Grey.
%\definecolor{stringcolor}{rgb}{0,0.6,0}        % Green.
%\definecolor{keywordcolor}{rgb}{0.8,0.0,0.6}   % Magenta.
%\definecolor{loremipsum}{rgb}{0.0,0.0,0.70}    % Dark Blue

%\definecolor{oldcolor}   {rgb}{0.6,0.6,0.7}
\definecolor{oldcolor}   {rgb}{0.4,0.4,0.6}  % a light blue-grey.
\definecolor{usedcolor}  {rgb}{0.7,0.2,0.2}  % a bland dark red.

% Default color is orange.
\definecolor{jb1}            {rgb}{0.65,0.35,1.0}   % purple.  regular, lightish, purple.
\definecolor{jbn}            {rgb}{0.45,0.55,1.0}   % It's basically periwinkle.
%
\definecolor{gg}             {rgb}{1.0,0.25,0.25}     % 
%
\definecolor{gs}             {rgb}{0.95,0.35,0.50}   % 


\definecolor{todoblue}       {rgb}{0.20,0.70,1.0}  % for things I'm conceptually confused about.
\definecolor{bluetodo}       {rgb}{0.20,0.70,1.0}  % for things I'm conceptually confused about.

\definecolor{organizational} {rgb}{0.7,0.95,0.10}   % Lime.  It's lime.
\definecolor{org}            {rgb}{0.7,0.95,0.10}   % Lime.  It's lime.

\definecolor{done}           {rgb}{0.6,0.6,0.8}    % a light blue-grey.

\definecolor{cleanup}        {rgb}{1.00,0.25,0.8}
\definecolor{clean}          {rgb}{1.00,0.25,0.8}
\definecolor{tag}            {rgb}{1.00,0.25,0.8}

\definecolor{moved}          {rgb}{0.3,0.3,0.3}
